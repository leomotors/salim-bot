// * Template for ShellConfig.json (Internal)

export interface ShellConfig {
    config: {
        disabled: string[]
    }
}

export const ShellConfigTemplate = {
    config: {
        disabled: []
    }
};
